# Core dependencies
torch>=1.13.0
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0
scikit-learn>=1.0.0
matplotlib>=3.5.0
tqdm>=4.62.0

# Molecular encoding
# Option 1: Uni-Mol (recommended)
# Install via: pip install unimol-tools
# Or from source: git clone https://github.com/dptech-corp/Uni-Mol.git
unimol-tools>=0.0.1

# Option 2: RDKit fallback (for fingerprints)
rdkit>=2022.3.1

# Optional: for development
pytest>=7.0.0
black>=22.0.0
isort>=5.10.0
